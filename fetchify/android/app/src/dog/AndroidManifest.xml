<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dog flavor specific permissions (same as GitHub for testing) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
</manifest>